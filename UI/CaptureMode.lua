local ADDON_NAME, MM = ...

local CaptureMode = {}
MM.ui.CaptureMode = CaptureMode

function CaptureMode:Start()
  MM:Print("capture mode is not implemented yet.")
end

function CaptureMode:Stop() end
